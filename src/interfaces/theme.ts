export interface ITheme {
	colors: {
		primary: string,
		secondary: string,
		info: string,
		success: string,
		warning: string,
		danger: string,
		dark: string,
		light: string,
		primaryText: string,
		secondaryText: string
	},
	breakpoints: {
		mobile: string,
		desktop: string
	}
}